# If you're using a `.env` file to store your secrets, do NOT commit it to GitHub.
# Instead, use this file to list the environment variables required by your project.
# Do NOT include real secrets, only variable names with placeholder values.

# Example of secrets:
LLM_AI_API_KEY=placeholder_value
DATABASE_URL=placeholder_value
